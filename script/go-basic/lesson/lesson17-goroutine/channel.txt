//=========== VD3:Khi khai báo size 1 (có buffer) bị lỗi (fatal error: all goroutines are asleep - deadlock!) vì buffer đã đầy
	ch := make(chan int, 3)

	ch <- 1
	ch <- 2
	ch <- 3
	close(ch)

	//go func() {
	//	defer close(ch)
	//	ch <- 1
	//	ch <- 2
	//	ch <- 3
	//}()

	for value := range ch {
		fmt.Println(value)
	}

	time.Sleep(1 * time.Second)